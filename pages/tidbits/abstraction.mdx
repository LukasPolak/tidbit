import NewsletterForm from '../../components/NewsletterForm'

# Abstraction

Abstraction is a generalization process - taking a concrete implementation and making it applicable to different, somewhat related data types.

<NewsletterForm />
