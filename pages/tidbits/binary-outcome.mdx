import NewsletterForm from '../../components/NewsletterForm'

# Binary outcome

A binary outcome is a term that implies there are only two possible outcomes for a particular situation. For example, in computer science, "bit" is a binary outcome - the value is either 0 or 1.

<NewsletterForm />
