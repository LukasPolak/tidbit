import NewsletterForm from '../../components/NewsletterForm'

# Tree shaking

Tree shaking means removing unused code. It depend on the import and export statements in ES6 to detect if code is imported between JavaScript files.

<NewsletterForm />
