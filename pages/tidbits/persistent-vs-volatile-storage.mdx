import NewsletterForm from '../../components/NewsletterForm'

# Persistent vs. Volatile storage

**Persistent storage** is a type of computer storage that retains data after power to that device is shut off.
‌

**Volatile storage** is a type of computer storage that needs the power to preserve stored data.

<NewsletterForm />
