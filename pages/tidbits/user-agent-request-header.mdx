import NewsletterForm from '../../components/NewsletterForm'

# User-Agent request header

The User-Agent request header is a string that lets servers identify the application, operating system, vendor, or version of the requesting user agent.

<NewsletterForm />
